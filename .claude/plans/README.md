# .claude/plans

Plan-mode output for this project. Set by `plansDirectory: "./.claude/plans"` in
`.claude/settings.json` (relative to the project root; the default would be `~/.claude/plans`).

Tracked in git on purpose so plans travel with the repo. Sort finished plans by outcome once they
land; keep the active one at the top level.
