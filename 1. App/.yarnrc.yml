# Overrides of Yarn 4 defaults required by this Electron app:
#   enableScripts - native deps (electron, electron-rebuild) need install/build scripts (default: false)
#   nodeLinker    - electron-builder + native modules require a real node_modules tree (default: pnp)
enableScripts: true

nodeLinker: node-modules
